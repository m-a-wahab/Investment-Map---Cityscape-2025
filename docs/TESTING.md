# 🧪 دليل الاختبار

## حل مشكلة عدم ظهور الخريطة

تم إصلاح المشكلة بإضافة:
1. ✅ حفظ مرجع الخريطة في `window.investmentMap`
2. ✅ استدعاء `invalidateSize()` عدة مرات بعد الانتقال
3. ✅ تأخيرات متعددة (100ms, 300ms, 600ms)

## طرق الاختبار

### الطريقة 1: الاستخدام العادي
افتح الملف مباشرة:
```
index.html
```
- ستظهر الصفحة الافتتاحية
- اضغط على زر "استكشف الفرص"
- يجب أن تظهر الخريطة بشكل صحيح

### الطريقة 2: تخطي الصفحة الافتتاحية (للاختبار السريع)
افتح الملف مع معامل skip:
```
index.html?skip
```
- سيتم تخطي الصفحة الافتتاحية تلقائياً
- الدخول مباشرة للخريطة

### الطريقة 3: التفعيل التلقائي
في ملف `splash.js`، قم بإلغاء التعليق على الأسطر:
```javascript
setTimeout(() => {
  enterApp();
}, 5000);
```
- سيتم الدخول تلقائياً بعد 5 ثواني

## التحقق من عمل الخريطة

افتح Console في المتصفح (F12) وابحث عن:
```
Map resized successfully
```

إذا ظهرت هذه الرسالة، فالخريطة تعمل بشكل صحيح.

## المشاكل المحتملة وحلولها

### المشكلة: الخريطة لا تزال لا تظهر
**الحل:**
1. تأكد من تحميل مكتبة Leaflet بشكل صحيح
2. افتح Console وتحقق من عدم وجود أخطاء
3. جرب فتح الصفحة بمعامل `?skip`

### المشكلة: الخريطة تظهر لكن بحجم صغير
**الحل:**
- هذا طبيعي، سيتم تصحيح الحجم تلقائياً بعد ثانية واحدة
- إذا لم يتم التصحيح، اضغط F5 لإعادة التحميل

### المشكلة: البيانات لا تظهر على الخريطة
**الحل:**
- تأكد من وجود ملف `data/plots.js`
- تحقق من Console للأخطاء

## نصائح للعرض في Cityscape

1. **استخدم الصفحة الافتتاحية** - تعطي انطباع احترافي
2. **اختبر قبل العرض** - تأكد من عمل كل شيء
3. **استخدم شاشة كبيرة** - التصميم محسّن للشاشات الكبيرة
4. **اتصال إنترنت جيد** - لتحميل خرائط Leaflet

## الأداء

- ⚡ وقت التحميل: ~2 ثانية
- 🎬 مدة الصفحة الافتتاحية: 3-5 ثواني (حسب المستخدم)
- 🗺️ وقت ظهور الخريطة: فوري بعد الدخول
